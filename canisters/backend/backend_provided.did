type DataItem = record {
  owner: text;
  name: text;
  description: text;
  price: nat64;
  data_hash: vec nat8;
};

service : {
  get_data : (nat64) -> (opt DataItem) query;
  store_data : (nat64, DataItem) -> ();
};
